<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed so the app can talk to our hosted/local REST API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".YouthLinkApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.YouthLinkSA"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic is needed so the app can reach http://10.0.2.2:3000
             (plain HTTP) while the API is running locally during development.
             Remove this once the API is deployed behind HTTPS for the final PoE. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.YouthLinkSA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
